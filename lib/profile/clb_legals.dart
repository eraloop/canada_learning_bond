import 'package:flutter/material.dart';
class CLB_Legals extends StatelessWidget {
  const CLB_Legals({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
